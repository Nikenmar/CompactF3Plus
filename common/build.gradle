plugins {
    id 'net.fabricmc.fabric-loom'
}

base {
    archivesName = "${mod_id}-common"
}

dependencies {
    minecraft "com.mojang:minecraft:${minecraft_version}"
    // No mod loader or API dependencies here, only Minecraft itself.
    // The Loom plugin provides the vanilla libraries and Mojang mappings natively.
}
